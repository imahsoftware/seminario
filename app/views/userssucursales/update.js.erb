$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

$("#userssucursal_<%= @userssucursal.id %>").replaceWith("<%= j render 'tr', userssucursal: @userssucursal %>");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
