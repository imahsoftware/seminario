$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

$("#userspermiso_<%= @userspermiso.id %>").replaceWith("<%= j render 'tr', userspermiso: @userspermiso %>");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
