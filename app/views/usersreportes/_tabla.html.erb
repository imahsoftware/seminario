<div class="table-responsive">
  <table class="table table-bordered table-condensed table-striped">
	  <thead>
	    <tr class="thcolor">
				<th width="10%"></th>
        <th>Reporte</th>
	    </tr>
	  </thead>
	  <tbody id="tbody_usersreportes">
	    <% @usersreportes.includes([:infgrupo]).order(updated_at: :desc).each do |usersreporte| %>
	      <%= render 'usersreportes/tr', usersreporte: usersreporte %>
	    <% end %>
	  </tbody>
	</table>
</div>
