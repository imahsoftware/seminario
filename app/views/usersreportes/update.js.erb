$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

$("#usersreporte_<%= @usersreporte.id %>").replaceWith("<%= j render 'tr', usersreporte: @usersreporte %>");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
