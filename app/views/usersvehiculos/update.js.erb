$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

$("#usersvehiculo_<%= @usersvehiculo.id %>").replaceWith("<%= j render 'tr', usersvehiculo: @usersvehiculo %>");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
