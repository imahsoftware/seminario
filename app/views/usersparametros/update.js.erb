$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

$("#usersparametro_<%= @usersparametro.id %>").replaceWith("<%= j render 'tr', usersparametro: @usersparametro %>");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
