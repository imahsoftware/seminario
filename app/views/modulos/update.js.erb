$(".alert_flash").fadeOut(1000, function() {
	$(this).remove();
});

$("#modulo_<%= @modulo.id %>").replaceWith("<%= j render 'tr', modulo: @modulo %>");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

setTimeout(function() {
	$(".alert_flash").fadeOut(1000, function() {
		$(this).remove();
	});
}, 5000);
