<div class="box box-primary">
  <div class="box-header with-border">
    <legend>
      Compañias
      <%= link_to new_portafolio_path, class: 'btn btn-sm btn-info' do %>
        <i class="fa fa-plus-square"> </i> <span>Nueva compañia</span>
      <% end %>
    </legend>
    <div class="box-tools pull-right">
      <%= search_form_for @q do |f| %>
        <div class="input-group input-group-sm" style="width: 180px;">
          <%= f.search_field :email_or_nombre_or_estado_cont, onChange: 'javascript:this.value=this.value.toUpperCase();', class: 'form-control pull-right input_uppercase', placeholder: 'Buscar...' %>
          <div class="input-group-btn">
            <%= button_tag(type: "submit", class: "btn btn-default") do %>
              <i class="fa fa-search"></i>
            <% end %>
          </div>
        </div>
      <% end %>
    </div><!-- /.box-tools -->
  </div><!-- /.box-header -->
  <div class="box-body">
    <div class="row">
      <div class="col-md-12">
        <div id="portafolios-form" hidden></div>
      </div>
    </div>
    <div class="row">
      <div class="col-md-12">
      <div class="box-body table-responsive">
        <table class="table table-bordered table-condensed table-striped">
          <thead>
            <tr class="thcolor">
              <th width="10%"></th>
              <th width="35%">Nombre</th>
              <th width="20%">Nombre Corto</th>
              <th width="20%">Forma VMN</th>
              <th width="10%"><div align="center">Estado</div></th>
            </tr>
          </thead>
          <tbody id="portafolios-tbody">
            <% @portafolios.order(:nombre).each do |portafolio| %>
              <%= render 'rows', portafolio: portafolio %>
            <% end %>
          </tbody>
        </table>
      </div>
      </div>
    </div>
  </div><!-- /.box-body -->
  <div class="box-footer clear-fix">
		<div class="box-tools pull-right">
			<%= will_paginate @portafolios, renderer: BootstrapPagination::Rails %>
		</div>
  </div><!-- box-footer -->
</div><!-- /.box -->
