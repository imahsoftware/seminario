$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

$("#usersvisita_<%= @usersvisita.id %>").replaceWith("<%= j render 'tr', usersvisita: @usersvisita %>");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
