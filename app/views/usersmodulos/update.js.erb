$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

$("#usersmodulo_<%= @usersmodulo.id %>").replaceWith("<%= j render 'tr', usersmodulo: @usersmodulo %>");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
