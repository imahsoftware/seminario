$("#eventosuser_form").collapse("hide");

$("#eventosuser_form").empty();

<% if @active_record.present? %>
	$("#eventosuser_<%= @active_record.id %>").replaceWith("<%= j render 'tr', eventosuser: @active_record %>");
<% end %>

$("#eventosuser_<%= @eventosuser.id %>").html("<td colspan='12'><%= j render 'form' %></td>");

$("#eventosuser_<%= @eventosuser.id %>").addClass("active_eventosuser");

$(".select2").select2();

$(".tooltip").tooltip("hide");

$('[data-toggle="tooltip"]').tooltip();
$(".datetimepicker").datetimepicker();
$('.datepicker').datepicker({
    dayNamesMin: [ "Dom", "Lun", "Mar", "Mie", "Jue", "Vie", "Sáb" ],
    monthNamesShort: [ "Ene", "Feb", "Mar", "Abr", "May", "Jun", "Jul", "Ago", "Sep", "Oct", "Nov", "Dic" ],
    dateFormat: "yy-mm-dd",
    yearRange: '2000:2027',
    changeMonth: true,
    changeYear: true,
});