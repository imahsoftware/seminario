$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

$("#eventosuser_<%= @eventosuser.id %>").replaceWith("<%= j render 'tr', eventosuser: @eventosuser %>");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
