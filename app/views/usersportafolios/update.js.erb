$(".alert_flash").fadeOut(300, function() {
	$(this).remove();
});

$("#usersportafolio_<%= @usersportafolio.id %>").replaceWith("<%= j render 'tr', usersportafolio: @usersportafolio %>");

$('[data-toggle="tooltip"]').tooltip();

$(".flash_messages").html("<%= j render 'layouts/flash_messages' %>");

setTimeout(function() {
	$(".alert_flash").fadeOut(300, function() {
		$(this).remove();
	});
}, 5000);
